# P3-R05 Review — P3-B06 R1 Add To Attack

- Document Role: INDEPENDENT_REVIEW_REPORT
- Owner: Codex R
- Review Task: `P3-R05`
- Implementation Task: `P3-B06`
- Exact Candidate: `f619df5479ed733b76c096410f7ccdc700abd661`
- Runtime Base: `c505c4748251feb9d151f4af52f091162a64a5b6`
- Status: `GATE_A_B_CANDIDATE_ACCEPTED`

## Findings

No blocking finding remains in the scoped B06 candidate.

The inherited ADD_TO_ATTACK implementation already provided the typed attachment primitive and production path, while this candidate closes a real legal-action projection gap: when no active non-controller target exists, the ability is no longer exposed and direct dispatch fails before cost or pending-state mutation.

## Independent verification

### Semantic / identity audit

Fresh runtime search found no `master.maiya`, `military.attach-support-shot`, or `master.maiya.deck.support-shot` card/ability identity route in the B06 runtime files. Routing is structural/semantic.

The existing scoped contract still carries the explicit cannot-win status semantic required by the B06 task; this review does not generalize that status to unrelated ADD_TO_ATTACK shapes.

### Fresh Gate A / focused suite

- `npm run typecheck`: PASS.
- ADD_TO_ATTACK inventory: eligible 1, skipped 6; legacy `1 -> 0`, new semantic route `0 -> 1`, dual `1 -> 0`.
- B06 + executable pack + data-flow + B04/B05 regressions: **61/61 PASS** across 5 files.

### Independent adversarial probes

R05 independently exercised authoritative state changes around the two-dispatch target flow.

No legal target remains:

`offered=false, ok=false, code=illegal_action, mana=6, revisionDelta=0, eventDelta=0, pending=false`.

Target becomes inactive after activation:

`activationOk=true, ok=false, code=illegal_target, mana=4, zone=skill, revisionDelta=0, eventDelta=0, pendingSame=true`.

Support card leaves skill after activation but before target resolution:

`activationOk=true, ok=false, code=resolution_failed, mana=4, zone=discard, revisionDelta=0, eventDelta=0, pendingSame=true`.

These results prove the first dispatch remains committed when valid, while an invalid second dispatch does not partially attach or mutate events/revision.

### Existing attack / PLAY counter separation

R05 independently placed an active pre-existing attack under target player `p2`, seeded nonzero normal-play counters for Maiya and p2, then resolved the attachment.

Result:

- activation/target resolution succeeds;
- Maiya mana `6 -> 4` (only the explicit Military ability cost);
- the pre-existing attack remains in `attack_area`;
- Support Shot is added alongside it under p2's control;
- the complete `playCounters` object remains unchanged.

This matches the canonical rule distinction between directly joining an attack and playing a card. B06 does not inherit normal PLAY counters or charge Support Shot's printed card cost.

### Gate C candidate evidence

Fresh reviewer Playwright run:

- Maiya add-to-attack browser / target selection / projection / reconnect / stale-command scenario: **3/3 PASS**.

This remains restored-snapshot scoped evidence; it is not a general ADD_TO_ATTACK or natural-match-progression certification.

### Full suite baseline

Fresh reviewer `npm run test:ci` result: **490 passed / 5 failed / 495 total**.

The five failures remain the same inherited environment/baseline classes:

- missing CHM/image evidence;
- the playtest pack reporting the same missing-image evidence set;
- two historical absolute `D:\\fd\\data\\manifests\\sample-cards.json` tests;
- the existing generated-content definition-hash mismatch.

No B06 focused/runtime regression failed.

## Evidence judgment

Accepted:

- exact Maiya ADD_TO_ATTACK semantic route;
- required skill-zone source validation;
- non-controller active-player target validation and no-target projection;
- attach beside an existing attack;
- direct attachment does not consume normal PLAY counters;
- only the explicit 2-mana ability cost is charged;
- stale target/source second-dispatch fail-closed behavior;
- scoped browser reconnect/stale-command candidate evidence.

Rejected / not claimed:

- Support Shot append-only or suppress semantics;
- round-end return cleanup promotion;
- normal PLAY, B05 response play, CREATE_AND_ACTIVATE, ACTIVATE, or CLOSE inheritance;
- broader Trigger/Lifecycle/Battle family acceptance;
- roster-wide migration, Phase 3 completion, or release readiness.

## Gate judgment

- Gate A: **PASS** for the exact B06 representative.
- Gate B: **PASS**, including independent target/source/counter adversarial verification.
- Gate C: scoped restored-snapshot candidate evidence **PASS**, without broader inheritance.

Permitted task completion token: `GATE_A_B_CANDIDATE_ACCEPTED`.

**Accepted P3-B06 runtime target: `f619df5479ed733b76c096410f7ccdc700abd661`.**
