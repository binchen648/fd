# P3-A01 Review Fix

Status: `REVIEW_ACCEPTED`
Owner: Codex A
Reviewed baseline: `1dcf31152f88ed25ea80a34c5abd1c9f643dc46d`
Accepted Candidate: `2c59d0623b8c2d3ee0de0f6fbd95692556316bbd`
Verdict source: coordinator-confirmed independent R re-review in the task thread on 2026-09-21

## Scope

This revision closes two evidence-automation blockers only. It changes no runtime semantics, primitive behavior, semantic routing, authoring card data, or Gate status.

## Review packet diff failure

`phase3-review-packet` now fails closed when neither `<diff-base>...HEAD` nor `<diff-base>..HEAD` can be evaluated. It throws before artifact creation and cannot report `hotRuntimeFilesTouched=NO` from an unknown diff.

Negative coverage includes:

- invalid Git ref throws a blocking error;
- CLI path does not write its requested packet when diff evidence is unavailable;
- manual CLI reproduction exits nonzero.

## Coverage artifact freshness

`artifacts/phase3-skill-coverage.json` was regenerated from the reviewed checkout. Its compiled content evidence is:

- definition hash: `5aa5a186bb201ce1f491cb6f38907a6267a4f30775113d9dd95651d58ba735d2`;
- compiled cards: `70`;
- compiled characters: `14`;
- blocking issues: `0`.

## Acceptance boundary

Independent R re-review accepted the exact Candidate above. This acceptance closes the two A01 automation blockers only; it does not promote Gate A/B/C, Phase 3, or Release status.
