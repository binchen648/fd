# P3-A R42 / FB2-16 Recovery Synchronization

Date: 2026-09-17
Role: Codex A
Status: `SYNCHRONIZED`

## Lineage

- Integrated current-main baseline: `553779e8ffcc926ae4763ee86a2ea937e090c128`.
- Fresh FM09 blocker synchronization: `5b50f1ad0b6054c84dd1bb2d65ddfba7fbfd81ea`.
- Fresh FB2-16 dependency-planning base: `ff0c9cb853d9b72273736d41ca85d8d1f08614fa`.
- Fresh FB2-16 recovery candidate: `bc45b2032ec344d2c743d8b32e3a3d05aa8b67ca`.
- Candidate direct parent is exactly `ff0c9cb853d9b72273736d41ca85d8d1f08614fa`; that base directly descends from current recovery blocker-sync lineage, not historical downstream acceptance lineage.
- Historical FB2-16/R42 commits such as `ed7d578...` remain technical evidence only and are not acceptance provenance for this recovery.

## Fresh R42 result synchronized

Fresh process-separated R42 review returns `IMPLEMENTATION_ACCEPTED_CANDIDATE` with no blocking finding for `bc45b2032ec344d2c743d8b32e3a3d05aa8b67ca`.

The reviewer independently verified the exact identity-free required-additional structural classifier, compiler/runtime shared predicate, malformed and foreign-marker fail-closed behavior, `master_skill` attack-area classification, standalone rejection, legal regular-batch and staged routes, ordinary/additional quota separation, aggregate payment rollback, effect-play isolation, Sieg compatibility, generated-content scope, zero migration credit, and final cleanliness.

Reviewer validation recorded:

- typecheck PASS;
- focused `4 files / 65 tests` PASS;
- full CI `128 files / 807 tests` PASS;
- rules core + regression `68 files / 411 tests` PASS;
- client production build PASS;
- content validation `0 blocking issues`;
- generated determinism PASS;
- locked Reference verification at `b2f9fa15fba07c63530bbf4612b03b8b704755f9` PASS;
- coverage remains `98 archives / 133 cards / 232 abilities`, runtime raw `22/3/127/0/80/124`;
- `git diff --check` PASS;
- reviewer and candidate worktrees both clean at the exact candidate SHA.

FB2-16 remains runtime/compiler dependency closure only. It takes zero frozen-migration credit. Accepted current-main overlap remains `111/944` (`11.76%`), leaving `833/944`.

## Fresh dependency scan

Fresh exact-ID scanning on the accepted candidate finds all twelve FM09 provisioning targets still absent from both canonical authoring and the generated loaded product definition set:

- `master.bazett.skill.s2`
- `master.caules-yggdmillennia.skill.s2`
- `master.caules-yggdmillennia.skill.s3`
- `master.ciel.skill.s2`
- `master.fujino.skill.s3`
- `master.shiki-nanaya.skill.s2`
- `master.shiki-ryougi.skill.s2`
- `master.shiki-ryougi.skill.s3`
- `master.shiki-tohno.skill.s2`
- `master.zouken.skill.s3`
- `master.zouken.skill.s4`
- `card.derived.master.shirou-emiya.ganjiang-moye`

The first eleven are separate frozen static identities inside the 944 denominator. The Shirou derived target is outside that frozen denominator.

Frozen F1 evidence still identifies `master.shirou-emiya.skill.s2` as the source skill whose game-start relationship adds the derived card. Locked Reference still supplies stable identity/static metadata for `card.derived.master.shirou-emiya.ganjiang-moye`. Fresh R42 supplies only the accepted required-additional execution shape; it does not authorize special identity routing or any frozen source migration.

## Dispatch decision

The narrowest legal next dependency is therefore a fresh zero-credit support-definition attempt for exactly `card.derived.master.shirou-emiya.ganjiang-moye`.

P3-FB2-17-RECOVERY may materialize only that non-frozen support definition using frozen F1 relationship evidence, locked Reference static metadata, and the accepted required-additional marker. It must not migrate Shirou s2, add any of the eleven frozen targets, change runtime/compiler code, weaken FB2-15 provisioning validation, or add identity/name/text/Reference-handler routing.

If the current representation cannot register the derived card outside game without incorrectly assigning an initial zone, S must return `SUPPORT_DEFINITION_BLOCKED`. No direct FM09 retry or Ciel task is authorized by this synchronization.
