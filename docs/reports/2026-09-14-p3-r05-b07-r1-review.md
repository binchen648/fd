# P3-R05 Review — P3-B07 R1 ACTIVATE Recovery

- Document Role: INDEPENDENT_REVIEW_REPORT
- Owner: Codex R
- Review Task: `P3-R05`
- Implementation Task: `P3-B07`
- Exact Candidate: `bd1d9ac73e42c21fb9078097313df259b2cb5738`
- Prior Rejected Candidate: `7981f5b9cdea6cdcf59168b599576740c4ddc6e6`
- Prior Rejection Evidence: `bf815157ed9a6298761f6beecae3927d2ed52164`
- Recovery Base: `bcc5d73e6064d750cc6b16e66ebb3dca00b69518`
- Mechanic Family: `CARD_ACTION_SEMANTICS_MINIMAL:ACTIVATE`
- Representative: Olga-Marie `astronomical-science.first-loss`
- Status: `GATE_A_B_CANDIDATE_ACCEPTED`

## Findings

No blocking finding remains in the scoped B07 r1 candidate.

The three blocking findings from the prior independent review are closed:

1. the production `MatchSession` battle path now derives the first-loss event from authoritative resolved battle history and supplies server-owned battle identity plus `lossOrdinal = 1`;
2. `activate_card_by_id` now fails closed on zero, duplicate, wrong-controller, wrong-zone, or already-active authoritative targets instead of selecting the first array match;
3. fresh committed Olga browser/server Gate C evidence is present and reproducible from the exact candidate.

The reviewer also verified the adjacent legality repair: Trismegistus is materialized as a unique skill-zone instance and is not exposed as a normal PLAY action before its ACTIVATE trigger.

## Independent Verification

### Semantic / identity audit

Fresh runtime search found no `master.olga-marie`, `astronomical-science.first-loss`, or `trismegistus-grief` identity branch in the ACTIVATE routing files. Eligibility remains semantic/structural.

`git diff --check 7981f5b..bd1d9ac` passes.

ACTIVATE inventory remains:

- semantic card-action abilities: 7;
- eligible ACTIVATE representative: 1;
- skipped: 6 with explicit out-of-scope reasons;
- legacy ACTIVATE consumer `1 -> 0`;
- new semantic route `0 -> 1`;
- dual-compatible ACTIVATE `1 -> 0`.

### Fresh Gate A / focused suite

- `npm run typecheck`: PASS.
- focused B07 / complex / data-flow / ADD_TO_ATTACK / Card Zone suite: **72/72 PASS** across 5 files after rerun.
- the first cold-start focused run had one unrelated 5-second timeout in the static 14-role ability-id matrix test; the exact test immediately reran PASS, and the complete 72-test focused suite then reran PASS. No B07 assertion failed.

The eight B07 regression tests cover:

- delayed first-loss staging and formal round-end activation;
- target-leaves-skill transactional rollback;
- duplicate delayed staging suppression;
- malformed first-loss event rejection;
- ambiguous duplicate target rejection;
- wrong-controller rejection;
- first-loss derivation exactly once from authoritative battle history;
- semantic classifier identity independence.

### Independent adversarial probe

R05 executed a separate temporary runtime probe rather than relying only on candidate tests. Observed result:

```text
compilerTarget=skill
activationOnlyNormalPlayBlocked=true
malformedEventRejected=true
authoritativeFirstLossStagesOnce=true
laterLossDoesNotRestage=true
duplicateTargetFailsClosed=true
```

The probe cleared the delayed queue after the first authoritative loss, inserted that loss into `battleHistory`, then presented a later loss. No second activation was staged, proving the history gate rather than duplicate-queue suppression is what blocks repeat first-loss emission.

For the duplicate-target adversarial case, round-end resolution rejected the ambiguous authoritative state while preserving the delayed activation and leaving both target instances in `skill`.

### Gate C evidence

Fresh reviewer Playwright run:

- Olga ACTIVATE browser/server scenario: **1/1 PASS**.

The scenario exercises a real room/session path from action phase through battle resolution and formal round end, confirms Trismegistus moves from `skill` to `field`, verifies projection after reconnect, and confirms a stale client command cannot mutate the settled revision or duplicate the activated card.

### Full rules baseline

Fresh reviewer `npx vitest run packages/rules/tests` result:

```text
40 passed files / 9 failed files
340 passed tests / 19 failed tests
```

The failure set matches the inherited baseline already documented by the implementer:

- 18 failures are missing historical CHM/original-image evidence paths in this environment;
- 1 failure is the existing `golden-card-content-pipeline.test.ts` source/generated definition-hash drift (`4c85668f...` fresh source versus self-consistent checked-in generated pack `f4aeaddb...`).

No focused B07/runtime test fails in the full run. This review does not promote or repair those inherited baseline problems.

## Evidence Judgment

Accepted for the scoped B07 candidate:

- semantic ACTIVATE routing without card/ability identity branching;
- authoritative first-loss production bridge derived from server battle history;
- stable battle identity and first-loss ordinal validation;
- delayed activation consumed only at formal `round_end`;
- unique authoritative definition-id target requirement;
- missing/duplicate/wrong-controller/wrong-zone/already-active fail-closed behavior;
- activation-only target excluded from normal PLAY;
- compiler materialization of the ACTIVATE target in `skill`;
- scoped browser projection/reconnect evidence.

Not claimed:

- broad Trigger gateway acceptance;
- generic delayed scheduler acceptance;
- other ACTIVATE semantic shapes;
- PLAY / PLAY_SOURCE / ADD_TO_ATTACK / CLOSE inheritance;
- battle-family, lifecycle, interaction, roster-wide, Phase 3, or release readiness;
- resolution of missing CHM/image evidence or generated-content source drift.

## Gate Judgment

- Gate A: **PASS** for the exact B07 ACTIVATE representative.
- Gate B: **PASS**, including independent authoritative-history and duplicate-target adversarial verification.
- Gate C: scoped real browser/server/reconnect evidence **PASS as candidate evidence**, without broader family inheritance.

Permitted task completion token: `GATE_A_B_CANDIDATE_ACCEPTED`.

**Accepted replacement B07 runtime target: `bd1d9ac73e42c21fb9078097313df259b2cb5738`.**
