# P3-R05 Review — P3-B08 R1 CLOSE Recovery

- Document Role: INDEPENDENT_REVIEW_REPORT
- Owner: Codex R
- Review Task: `P3-R05`
- Implementation Task: `P3-B08`
- Exact Candidate: `5d34befa085fe6426e1d550ad0c5d0a0ca824db6`
- Accepted Runtime Base: `bd1d9ac73e42c21fb9078097313df259b2cb5738`
- Mechanic Family: `CARD_ACTION_SEMANTICS_MINIMAL:CLOSE`
- Representative: Artoria Alter `sc-artoria-alt-2.angra-mainyu-embrace`
- Status: `GATE_A_B_CANDIDATE_ACCEPTED`

## Findings

No blocking finding remains in the scoped B08 r1 candidate.

The recovered CLOSE slice is now repacked on the fresh accepted B07 replacement base rather than the obsolete rejected B07 chain. The exact semantic route remains narrow: a residual `on_card_played` trigger with source-on-board and played-card `宝具` conditions, no targets/cost/creates, and exactly one `close_source_card` effect.

## Independent Verification

### Identity / semantic audit

Fresh reviewer inventory reports:

```text
cardActionSemanticAbilities=7
eligible=1
skipped=6
legacyCloseConsumerCount.before=1
legacyCloseConsumerCount.after=0
newRuntimeSemanticRoutedCloseCount.before=0
newRuntimeSemanticRoutedCloseCount.after=1
dualCompatibleCloseCount.before=1
dualCompatibleCloseCount.after=0
```

The sole eligible representative is Artoria Alter `sc-artoria-alt-2.angra-mainyu-embrace`. The six non-CLOSE card-action shapes remain explicitly out of scope. Runtime routing contains no Artoria/card-id/ability-id equality branch.

`git diff --check` passes.

### Fresh Gate A / focused suite

- `npm run typecheck`: PASS.
- CLOSE / ACTIVATE / ADD_TO_ATTACK / data-flow / Card Zone / complex regression suite: **77/77 PASS** across 6 files.
- dedicated B08 CLOSE regression: 5/5 PASS.

Coverage includes:

- canonical noble-phantasm play closes the active source through typed data-flow;
- non-noble play does not close it;
- inactive source fails without mutation;
- face-down source fails without mutation;
- semantic classifier rejects wrong trigger/attribute/target/extra-effect shapes.

### Independent adversarial probe

R05 separately corrupted authoritative source state and called the typed resolution boundary rather than relying only on candidate tests.

Observed:

```text
wrongControllerFailsClosed=true
offBoardFailsClosed=true
callerStatePreserved=true
```

A wrong-controller source and an off-board source both reject before commit. The caller state remains byte-for-byte equivalent through the clone/commit transaction boundary.

### Fresh Gate C evidence

Reviewer independently ran:

```text
npm run e2e:fd-remote -- e2e/fd-artoria-alt-close-card-action.spec.ts
PASS: 1 / 1
```

The browser/server scenario restores a real remote room with Artoria Alter's CLOSE source active and her canonical noble phantasm legally playable from `skill`. A WebSocket `client:dispatch_command` with `expectedRevision` plays the noble phantasm. The server resolves the residual CLOSE trigger, projects the source back to `skill`, keeps the noble phantasm in `attack_area`, preserves that state across reconnect, and rejects stale replay without a second projected CLOSE effect.

The public room log projects the CLOSE ability as its `effect_resolved` event; the internal `source_card_closed` event remains authoritative runtime evidence and is covered by the focused regression. This review does not require internal-only events to be leaked through the player projection.

### Full rules baseline

Fresh reviewer `npx vitest run packages/rules/tests` result:

```text
41 passed files / 9 failed files
345 passed tests / 19 failed tests
```

The failure set matches the inherited accepted B07-base pattern:

- 18 failures depend on unavailable historical CHM/original-image evidence paths in this environment;
- 1 failure is the existing source/generated definition-hash drift in `golden-card-content-pipeline.test.ts` (`4c85668f...` fresh source versus `f4aeaddb...` checked-in generated pack).

No B08 focused/runtime test fails in the full run.

## Evidence Judgment

Accepted for the scoped B08 candidate:

- semantic CLOSE routing without identity branching;
- active face-up source validation;
- controller ownership validation;
- `field` / `attack_area` source-zone validation;
- typed `closedCount` result and authoritative `source_card_closed` emission;
- source movement to `skill`, owner-only visibility, and inactive face-up state;
- non-noble and corrupted-state fail-closed behavior;
- browser projection/reconnect/stale-replay candidate evidence.

Not claimed:

- targeted close;
- close-then-activate;
- response-window close;
- temporary-card dissolve;
- non-skill close destinations;
- hidden/private or face-down identity proof;
- broad Lifecycle cleanup ordering;
- roster-wide, Phase 3, or release readiness;
- resolution of missing CHM/image evidence or generated-content drift.

## Gate Judgment

- Gate A: **PASS** for the exact B08 CLOSE representative.
- Gate B: **PASS**, including independent wrong-controller/off-board fail-closed verification.
- Gate C: scoped browser/server/reconnect evidence **PASS as candidate evidence**, without broader inheritance.

Permitted task completion token: `GATE_A_B_CANDIDATE_ACCEPTED`.

**Accepted replacement B08 runtime target: `5d34befa085fe6426e1d550ad0c5d0a0ca824db6`.**
