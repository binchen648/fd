# P3-TO-14 Independent Spec Review

- Date: 2026-09-14
- Candidate exact SHA: `e7c1df2830f8f6c9e277dcdb4cf4341a1f6491f5`
- Base: `3460b0a1fc5b1bf974847c62f29a264e28329f7b`
- Reviewer worktree: `E:\Codex\FD\fd-r-to14`
- Final: `SPEC_NEEDS_REVISION`
- Blocking findings: 1

## What passed

- Candidate scope is docs-only: 4 added docs files, no runtime edits.
- Current corrected semantic-axis BATTLE_INTEGRATION denominator is exactly 39 abilities / 28 cards.
- Historical BATTLE_RESULT membership and current 39-row semantic-axis set are exact-set equal.
- Exactly 13 of those 39 rows consume the five post-result/battle-ended events listed by the candidate.
- The candidate correctly refuses to relabel the remaining 26 battle-integration rows as direct Battle Result consumers.
- Power -> Battle Result -> Trigger -> Scoring -> Resource ownership is otherwise separated cleanly.
- Scoring plan is immutable and separate from trigger-earned Resource results.
- No arbitrary TypeScript `unknown` payload or runtime migration claim is present.

## Blocking finding

### [P1] Scoring/Battle-owned `after_controller_gains_victory` producer contract is missing

The accepted Trigger Gateway contract lists `after_controller_gains_victory` as a strict domain event whose external producer owner is **Scoring/Battle runtime** and whose minimum payload includes controller, source battle/result identity, and VP/victory causation as applicable.

Current authoring has one strict consumer:

- card: `servant.artoriac.skill.sc-artoriac-3`
- ability: `sc-artoriac-3.shuffle-discard-on-victory`
- trigger: `after_controller_gains_victory`
- printed clause: after the controller gains a victory, shuffle the discard pile into the deck; the effect cannot be prevented.

This row is **not** tagged BATTLE_INTEGRATION in the current semantic-axis, so it must **not** be added to the 39/28 denominator merely to close the producer gap. However, P3-TO-14 owns the Battle/Scoring event-producer boundary, so its contract must still define how and when Scoring/Battle produces this accepted Trigger Gateway event.

The current TO-14 event table covers result-determined, win, loss, first-loss, and battle-ended events but omits `after_controller_gains_victory`. Its stage model also goes directly from scoring commit to `after_battle_ended`, leaving no authoritative stage for this scoring-derived event.

Required repair:

1. Keep the BATTLE_INTEGRATION denominator at 39/28 and direct result/ended subset at 13.
2. Add an explicit cross-axis producer dependency for the one `after_controller_gains_victory` trigger consumer.
3. Define its minimum payload/identity and exactly-once causation linked to Battle Result + scoring/victory settlement.
4. Insert its event/trigger settlement in the stage model after the relevant scoring/victory commit and before terminal `after_battle_ended` / cleanup, unless an accepted rule explicitly requires another order.
5. Do not define “gains victory” as arbitrary positive VP or infer it from display text; qualification must be a reviewed scoring/battle rule input.
6. Extend reviewer checklist/result notes so future reviewers cannot silently lose this cross-axis producer again.

## Decision

The 39-row denominator and main envelope structure are accepted as evidence, but producer coverage is incomplete. Candidate cannot be `SPEC_ACCEPTED` until the missing Scoring/Battle strict event owner is closed.
