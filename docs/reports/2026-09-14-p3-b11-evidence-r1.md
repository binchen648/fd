# P3-B11 Evidence Repair r1

- Document Role: RUNTIME_EVIDENCE_REPAIR
- Owner: Codex B
- Task: `P3-B11`
- Base Target: `ddaf9023e214f7a7fe6ff750f58f17c04290ed06`
- Triggering Review: `a345cbe` / P3-R06 `IMPLEMENTATION_NEEDS_REVISION`
- Scope: first-stage rollback regression only
- Runtime Change: none
- Status: `IMPLEMENTATION_COMPLETE_CANDIDATE`

## Repair

P3-R06 independently verified that Golden Eater's first-stage typed runtime failure already rolls back correctly, but the exact failure path was not durable in the committed focused suite.

This repair adds one production regression that:

1. opens Golden Eater's first private target decision through real `MatchSession.dispatchPlayerAction`;
2. simulates a trusted server-side race in which candidate discovery sees the target in `hand` while the typed `move_card` primitive still requires `removed_from_game -> skill`;
3. dispatches the first `choose_target`;
4. requires `resolution_failed`;
5. asserts complete authoritative state equality against the pre-dispatch snapshot;
6. separately asserts revision unchanged, events unchanged, first pending decision preserved, and no card movement committed by the failing dispatch.

No runtime, compiler, classifier, authoring, E2E, KPI, taxonomy, or coverage file is changed by r1.

## Verification

```text
npm run typecheck
PASS

npx vitest run \
  packages/rules/tests/executable-card-pack.test.ts \
  packages/rules/tests/regression/resolution-dataflow.test.ts \
  packages/rules/tests/regression/production-resolution-bridge.test.ts \
  packages/rules/tests/regression/card-zone-core-direct-action.test.ts \
  packages/rules/tests/regression/complex-skills-regression.test.ts
PASS: 5 files / 91 tests

git diff --check
PASS
```

Full rules comparison after adding the single evidence test:

```text
383 total
364 passed
19 failed
```

The 19 failures are the same inherited non-B11 baseline set documented by the B11 implementation report.

`test:ci` after the evidence-only addition:

```text
504 total
499 passed
5 failed
```

The five failures remain the same inherited CHM/image-path, historical sample-manifest absolute-path, and generated-content-hash failures outside B11.

## Diff Boundary

Against `ddaf902`, the only functional repository change is:

- `packages/rules/tests/regression/production-resolution-bridge.test.ts`

This report is the only additional documentation file.

## Handoff

Fresh P3-R06 review should verify the exact r1 target and confirm that the prior first-stage rollback evidence finding is closed. No Gate status is promoted by this B-lane repair.
