# P3-R05 Review — P3-B05 R2 Response Play

- Document Role: INDEPENDENT_REVIEW_REPORT
- Owner: Codex R
- Review Task: P3-R05
- Implementation Task: P3-B05
- Exact Candidate: `c505c4748251feb9d151f4af52f091162a64a5b6`
- Prior Rejected Candidate: `d03cde2f24b569222b3bd39b57f60dee610c7aa5`
- Prior Review Finding: `b2e8db355266bfa9ff988d0a46f3c07b1af29c44`
- Status: `GATE_A_B_CANDIDATE_ACCEPTED`

## Findings

No blocking finding remains in the R2 candidate.

The P1-equivalent legality gap from the first R05 review is closed: effect-driven `play_source_card` no longer bypasses shared card-play forbids. The candidate reuses the shared play path instead of adding Kayneth, Volumen, card-id, or ability-id routing.

## Independent verification

### Semantic / identity audit

A direct search of the B05 runtime routing code found no `master.kayneth`, `volumen.extra-play`, or `volumen-hydrargyrum` identity branch. Eligibility remains structural/semantic.

The repaired route now:

- calls shared `playFailure` when projecting response legality, with only effect-authorized timing/quota exceptions;
- calls shared `playBatch(..., 'effect')` for the typed source-card play;
- still requires source-in-hand and fixed-cost availability;
- accounts for face-up printed card cost in addition to the fixed ability cost.

### Fresh Gate A / focused suite

- `npm run typecheck`: PASS.
- B05/B04/data-flow/executable-pack/complex focused run: **87/87 PASS** across 5 files.

The six B05 response tests include persistent regressions for:

- successful Volumen response play;
- insufficient mana / source no longer in hand;
- source-zone stale action rejection;
- post-window mana drop rejection with zero commit;
- post-window shared attribute play-forbid rejection with zero commit;
- exact semantic classifier boundaries without identity routing.

### Independent adversarial probe

R05 independently captured a legal response action and then mutated authoritative server state before dispatch.

Shared play-forbid inserted after window open:

`ok=false, code=illegal_response, mana=5, zone=hand, eventDelta=0, revisionDelta=0, windowDelta=0`.

Mana reduced to 1 after window open:

`ok=false, code=illegal_response, mana=1, zone=hand, eventDelta=0, revisionDelta=0, windowDelta=0`.

This directly reproduces the first-review attack shape and confirms the blocker is closed without partial state commit.

### Gate C candidate evidence

Fresh reviewer Playwright run:

- Volumen browser / WS / reconnect / projection / stale-command scenario: **3/3 PASS**.

This remains restored-snapshot scoped evidence. It does not by itself prove every natural battle progression path or promote wider Card Action contracts.

### Full suite baseline

Fresh reviewer `npm run test:ci` has the same inherited five failure classes already present before R2:

- missing reviewed CHM/image evidence;
- playtest-pack loader reporting the same 93 missing-image evidence entries;
- two tests using the historical absolute `D:\\fd\\data\\manifests\\sample-cards.json` path;
- the existing generated-content definition-hash mismatch.

No B05 focused/runtime test failed. These environment/baseline issues are not promoted or fixed by R05.

## Evidence judgment

Accepted:

- fixed 2-mana response contract;
- source-card-in-hand revalidation;
- server-side stale mana revalidation;
- shared card-play forbid inheritance;
- semantic classifier without identity routing;
- shared play execution path and play-trigger/counter semantics;
- browser reconnect and stale-revision candidate evidence.

Rejected / not claimed:

- inheritance from B05 to normal PLAY, ADD_TO_ATTACK, ACTIVATE, CLOSE, or CREATE_AND_ACTIVATE;
- roster-wide migration;
- Phase 3 completion;
- release readiness;
- resolution of the unrelated missing-evidence / absolute-path / generated-hash baseline failures.

## Gate judgment

- Gate A: **PASS** for the scoped B05 candidate.
- Gate B: **PASS** for the scoped B05 candidate, including independent adversarial server revalidation.
- Gate C: scoped restored-snapshot production evidence **PASS as candidate evidence**, without broader promotion.

Permitted task completion token: `GATE_A_B_CANDIDATE_ACCEPTED`.

**Accepted replacement B05 runtime target: `c505c4748251feb9d151f4af52f091162a64a5b6`.**
