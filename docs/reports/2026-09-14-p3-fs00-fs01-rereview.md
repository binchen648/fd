# P3-FS00 / P3-FS01 Independent Re-review

- Date: 2026-09-14
- Reviewer branch: `codex/r-p3-fs00-fs01-rereview`
- Reviewed candidate: `8886546f43df1e84579aed841fe0b34b9a645812`
- Candidate branch / PR: `codex/s-p3-fs00-reference-lock` / PR #4
- Current-main commit merged into candidate: `fba31b5a725e6c0b8ba54793be8b6726bfc31040`
- Locked Reference commit: `b2f9fa15fba07c63530bbf4612b03b8b704755f9`
- Reference root used for reproduction: local clean checkout `E:\Codex\FD\fd-reference` (not recorded in generated evidence)

## Scope

This re-review answers the blocker raised against PR #4: the old candidate had fallen behind `origin/main` and could regress already-accepted CI/path/content fixes. The author merged current `origin/main` into the PR branch before this review. This reviewer started from the exact post-merge candidate in a fresh worktree and did not modify implementation files.

The unique candidate diff relative to current `origin/main` contains only:

- `data/phase3/full-roster-ability-inventory.json`
- `package.json`
- `scripts/phase3-reference/build-full-roster-inventory.ts`
- `scripts/phase3-reference/inventory-schema.ts`
- `scripts/phase3-reference/types.ts`
- `scripts/phase3-reference/verify-reference.ts`
- `scripts/tests/phase3-full-roster-inventory.test.ts`
- `scripts/tests/phase3-reference-lock.test.ts`

`package.json` changes relative to current `origin/main` only add `phase3:reference:verify` and `phase3:reference:intake`; they do not alter the current CI command or revert path/content fixes.

## Independent reproduction

### Clean install and compilation

```text
npm ci
npm run typecheck
```

Result: PASS.

### FS00 / FS01 focused tests

```text
npm exec vitest run scripts/tests/phase3-reference-lock.test.ts scripts/tests/phase3-full-roster-inventory.test.ts
```

Result: PASS, 2 files / 16 tests.

Coverage includes wrong commit, dirty checkout, required input closure, missing local dependency, missing inputs, portable metadata, output-root protection, Windows path case variants, junction/symlink output aliases, 943 static identities, 1 separate dynamic identity, deterministic ordering, silent omission rejection, and duplicate identity rejection.

### Current CI baseline

```text
npm run test:ci
```

Result: PASS, 80 files / 461 tests.

The previous blocker is closed: the refreshed candidate no longer fails `test:ci` because of a baseline rollback.

### Diff hygiene

```text
git diff --check
```

Result: PASS.

## Locked Reference verification

Fresh reviewer execution:

```text
npm run phase3:reference:verify -- --reference-root <clean-reference-checkout>
```

Result: PASS.

Verified repository:

```text
https://github.com/fengling20011118-dotcom/fate-domination.git
```

Verified commit:

```text
b2f9fa15fba07c63530bbf4612b03b8b704755f9
```

The verifier also reproduced deterministic SHA-256 digests for the required files and the transitive local TypeScript input closure. The Reference checkout remained clean.

## FS01 inventory reproduction

The reviewer generated the inventory twice to two independent temporary output files and compared both files with the checked-in inventory.

All three SHA-256 values were identical:

```text
695000f27ad8bc22cdf19558ea172ddc4a40029fd5204f51602a65c67e29b0bb
```

Reproduced totals:

```text
staticSkills  = 943
dynamicSkills = 1
```

The dynamic identity remains separate from the static roster. The checked-in inventory contains no local `C:\`, `D:\`, `E:\`, `/Users/`, or `fd-reference` path leakage.

## Independent output-guard adversarial probe

The reviewer separately exercised `assertOutputOutsideReference` rather than relying only on the candidate-authored tests.

Result:

```json
{"caseRejected":true,"junctionRejected":true,"outsideAllowed":true}
```

Therefore:

- a Windows path-case alias into Reference is rejected;
- a junction alias into Reference is rejected;
- a genuine path outside Reference remains allowed.

The temporary probe was deleted after execution and the reviewer worktree returned clean before this report was added.

## Findings

No blocking FS00/FS01 finding remains on candidate `8886546f43df1e84579aed841fe0b34b9a645812`.

The previously reported current-main regression blocker is closed because the candidate now contains current-main CI/path/content fixes and independently passes the current CI baseline.

## Judgment

- **FS00: ACCEPTABLE for the full-roster intake chain.** The locked Reference repository/commit, clean status, required input closure, digests, read-only output boundary, and portable evidence are reproducible.
- **FS01: ACCEPTABLE for the full-roster intake chain.** The inventory deterministically accounts for 943 unique static skills plus the one known dynamic skill, with no checked-in drift against a fresh generation.
- **F0: NOT PROMOTED BY THIS REPORT.** Final F0/F1 checkpoint acceptance remains owned by the later FA01/FR01 full-roster audit/review route defined by the collaboration contract.

This report does not authorize runtime migration and does not promote any runtime Gate.
