# P3-A R97 FB2-46 Acceptance Synchronization

Role: Codex A
Status: `SYNCHRONIZED`
Date: 2026-09-21

## Accepted capability input

- Formal verdict: `IMPLEMENTATION_ACCEPTED_CANDIDATE`
- Canonical reviewer evidence: `https://github.com/binchen648/fd/pull/410#issuecomment-5753024529`
- Exact PR dispatch Base: `4a31e42c0a4b1c687badba8161503313d6606e75`
- Accepted B2 Candidate: `43ba2b1b7fc8dc53a9602b70dcf869f50784a58d`
- Prior rejected Candidate: `2c4d46f8a3df3ffdc9e48f992308ae5c7ecdfb49`
- Prior needs-revision evidence: `https://github.com/binchen648/fd/pull/410#issuecomment-5752950621`
- PR: `#410`
- Task: `P3-FB2-46-BATTLE-LOSS-VP-WINNER-REWARD`
- Branch: `codex/b2-p3-fb2-46-battle-loss-vp-winner-reward`
- Locked Reference: `b2f9fa15fba07c63530bbf4612b03b8b704755f9`

The plugin handoff supplied only `http` as `evidenceRef`; that fragment is not usable evidence. A mechanically recovered the canonical exact-Candidate reviewer comment `5753024529` directly from PR #410 and uses only that formal evidence here.

## A synchronization checks

A mechanically rechecked the exact reviewed pair and canonical R evidence:

- this synchronization worktree starts at exact accepted Candidate `43ba2b1b7fc8dc53a9602b70dcf869f50784a58d`;
- PR #410 remains OPEN / CLEAN / non-draft and unmerged, with exact Base `4a31e42c0a4b1c687badba8161503313d6606e75`, exact Head `43ba2b1b7fc8dc53a9602b70dcf869f50784a58d`, unchanged Base branch `codex/a-p3-fb2-46-battle-loss-vp-winner-reward-dispatch`, and unchanged Head branch `codex/b2-p3-fb2-46-battle-loss-vp-winner-reward`;
- canonical fresh independent R evidence comment `5753024529` binds PR #410, exact Base, exact Candidate and task, and returns terminal verdict `IMPLEMENTATION_ACCEPTED_CANDIDATE`;
- the prior exact Candidate `2c4d46f8a3df3ffdc9e48f992308ae5c7ecdfb49` was independently rejected only for the raw `markers` fail-closed gap; the accepted revision closes that finding with one classifier guard plus focused scalar / true-name / empty-marker regressions and does not widen loader/runtime settlement behavior;
- Base-to-Candidate scope remains exactly six authorized paths: the FB2-46 result report, dedicated battle-loss VP winner-reward classifier/provenance helper, loader integration, interpreter integration, rules index export, and focused FB2-46 tests;
- accepted semantics remain only the bounded identity-free `battle_loss_vp_then_reward_winners` transaction on a trusted `after_controller_loses_battle` event, using the same frozen battle result, actual positive VP-loss gating, fixed per-winner reward, atomic fail-closed provenance checks and existing processed-event idempotence;
- raw generic `lose_victory_points`, `event_combat_winners` and `thenIfAnyLost` are not exposed as generic executable authoring;
- Base-to-Candidate contains no `data/authoring/**`, `data/packs/**`, client production, generated product, or coverage-artifact delta and adds no Nobunaga/name/Chinese identity routing in production rules code;
- therefore synchronized material authoring overlap remains **`144/944`**, duplicates **`0`**; no consumer identity is added by FB2-46 acceptance synchronization;
- fresh R records typecheck PASS, focused compatibility PASS (`7 files / 52 tests`, FB2-46 `9/9`), independent marker gateway probe PASS (`3/3`), `git diff --check` PASS, and clean detached reviewer worktree;
- no merge, retarget, consumer authoring or migration credit is authorized or performed here.

## Formal accounting after synchronization

Project formal migration accepted remains **`149/944`**, with **`795`** remaining.

FB2-46 is identity-free runtime capability infrastructure and earns **zero migration credit**. Branch-local material authoring overlap remains **`144/944`**, duplicates **`0`**; material overlap is evidence only and is not substituted for formal accepted migration credit.

PR #410 remains OPEN, unmerged and unretargeted.

## Next coordinator action

Freshly reconstruct the complete `servant.nobunaga.skill.sc-nobunaga-3` card against this exact synchronized runtime. Do not assume FB2-46 makes the card S-ready and do not add `player.defeated` semantics by implication. Dispatch exactly one singleton S only if the complete card is mechanically zero-gap across its full frozen authoring envelope and runtime behavior. If `player.defeated` or any other whole-card gap remains, record the exact residual blocker and continue migration-credit-first probing before releasing any additional zero-credit capability task.
